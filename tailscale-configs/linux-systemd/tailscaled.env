# Tailscale Environment Configuration
# Place this file at: /etc/default/tailscaled

# Point to your Headscale server
TS_LOGIN_SERVER=http://localhost:8000

# Optional: Pre-auth key (generate with: docker exec headscale headscale preauthkeys create --user 1 --reusable --expiration 24h)
# TS_AUTHKEY=your-preauth-key-here

# State directory
TS_STATE_DIR=/var/lib/tailscale

# Optional: Set hostname
# TS_HOSTNAME=my-device

# Optional: Advertise routes (comma-separated)
# TS_ROUTES=192.168.1.0/24,10.0.0.0/24

# Optional: Accept routes from other nodes
# TS_ACCEPT_ROUTES=true

# Optional: Advertise as exit node
# TS_EXIT_NODE=true

# Optional: Enable SSH
# TS_SSH=true
